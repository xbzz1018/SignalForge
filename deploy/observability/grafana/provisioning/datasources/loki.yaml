apiVersion: 1

datasources:
  - name: QuantPilot Loki
    uid: quantpilot-loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: true
    jsonData:
      maxLines: 1000
